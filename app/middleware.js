/**
 *  App Check Middleware
 */

// Dependencies


// Container for all the middleware
var lib = {};

// App check the token in headers route
lib.sessionChecked = function (data, callback) {
    console.log(123);
};


// Export the module
module.exports = lib;